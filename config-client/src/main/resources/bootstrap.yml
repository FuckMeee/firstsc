spring:
  profiles:
    active: dev
---
spring:
  profiles: prod
  application:
    name: spring-cloud-config-client
  cloud:
    config:
      uri: http://localhost:10001/
      name: config
      profile: dev
      label: master
---
spring:
  profiles: dev
  application:
    name: spring-cloud-config-client
  cloud:
    config:
      uri: http://localhost:10001/
      name: config
      profile: dev
      label: master